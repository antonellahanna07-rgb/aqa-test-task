import type { RawEnvConfig } from '../schema';

const config: RawEnvConfig = {
  baseUrl: 'https://vikunja.dev.example.com',
  apiBaseUrl: 'https://vikunja.dev.example.com/api/v1',
};

export default config;
