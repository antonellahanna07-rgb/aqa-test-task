import type { EnvConfig } from './manager';

const staging: EnvConfig = {
  baseUrl: 'https://vikunja.staging.example.com',
  apiBaseUrl: 'https://vikunja.staging.example.com/api/v1',
};

export default staging;
